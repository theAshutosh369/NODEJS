

const add = (a,b )=>{ return a+b; }

const sub = (a, b) => {
  return a - b;
};

const mul = (a, b) => {
  return a * b;
};

const div = (a, b) => {
  return a / b;
};

const name = 'Ashutosh';


module.exports = { add , sub , mul , div , name };
// module.exports = { } we have to add the variable or functions or anything that is to be exported. thats it you have created your own module !! 